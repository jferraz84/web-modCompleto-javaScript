const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);
// .toFixed limita a quantidade de números dps do ponto = Exe : 98.545
console.log(area.toFixed(3));
// Tipo do Math = object
console.log(typeof Math);